// // Ask the wizard their wand core (e.g., "phoenix") and print it in all lowercase and uppercase.

var wandCore = prompt("What is your wand core?");
console.log(wandCore.toLowerCase());
console.log(wandCore.toUpperCase());
